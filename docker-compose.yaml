# HelloIAM Stories — локальный Docker

services:
  helloiam-studio_v0.0.1:
    container_name: helloiam-studio_v0.0.1
    image: helloiam-studio:v0.0.1
    build: .
    restart: unless-stopped
    ports:
      - "0.0.0.0:3456:3456"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      GALLERY_PORT: 3456
      NODE_ENV: production
    volumes:
      - ./.env:/app/.env:ro
      - ./public/generated:/app/public/generated
      - ./public/emoji-style-reference.png:/app/public/emoji-style-reference.png:ro
      - ./out/renders:/app/out/renders
      - ./data/posts:/app/data/posts
      - ./data/story-templates:/app/data/story-templates
      - ./data/changelog.json:/app/data/changelog.json
      - ./src/templates:/app/src/templates
      - ./src/lib:/app/src/lib
      - ./src/gallery:/app/src/gallery
      - ./scripts:/app/scripts
      - ./tools:/app/tools
      - "./ALL EMOJIS:/app/ALL EMOJIS"
      - "./ALL CARDS:/app/ALL CARDS"
    shm_size: "2gb"
    mem_limit: 3g
